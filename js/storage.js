/*
  storage.js - localStorage 読み書き共通処理

  TODO: ここに実装する予定の関数

  - saveFramework(id, data)
      フレームワークの入力内容をlocalStorageに保存する
      キー形式: "lf_" + id（例: "lf_swot"）

  - loadFramework(id)
      指定IDの保存データをlocalStorageから読み込む
      データが存在しない場合は null を返す

  - clearFramework(id)
      指定IDの保存データを削除する

  - exportAsJSON(id)
      保存データをJSONファイルとしてダウンロードする（将来的な共有機能用）
*/
